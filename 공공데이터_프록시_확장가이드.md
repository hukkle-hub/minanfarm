# 단점 보완 가이드 — 농장번호 전체 조회를 위한 확장 프록시

이 문서는 minanfarm 앱이 축산물이력제를 실제로 연동할 때 마주치는 두 가지 단점을 보완하기 위한 확장 프록시의 구성 방법을 정리한 것입니다. 앞서 작성한 기본 배포 가이드(공공데이터_프록시_배포가이드.md)가 프록시의 기본 구조를 다루었다면, 이 문서는 그 위에 농장 단위 전체 조회 기능을 더하는 방법에 집중합니다.

## 보완하려는 두 가지 단점

첫 번째 단점은 브라우저의 교차 출처 차단입니다. 공공데이터포털 API는 다른 출처의 웹페이지에서 직접 호출하는 것을 차단하므로, 미리보기나 웹 브라우저에서는 키가 정확하더라도 응답을 받지 못합니다. 이 문제는 프록시 경유만으로 이미 해결됩니다.

두 번째 단점은 농장 단위 공개 조회가 제공되지 않는다는 점입니다. 쇠고기이력정보 서비스는 개체식별번호로 한 마리씩 조회하는 방식이며, 농장번호 하나로 그 농장의 전체 개체를 한꺼번에 내려주는 공개 오퍼레이션은 없습니다. 이 문서의 핵심은 바로 이 두 번째 단점을 프록시가 대신 보완하도록 만드는 것입니다.

## 보완 원리

개체식별번호 열두 자리는 국가코드 세 자리와 농장식별 구간, 그리고 개체별 일련번호로 구성됩니다. 같은 농장에서 출생한 개체들은 농장식별 구간을 공유하므로, 프록시가 그 구간을 고정한 채 일련번호 범위를 순회하며 개체 조회를 반복하면, 실제로 존재하는 개체만 모아 농가 전체 목록을 구성할 수 있습니다. 앱은 농장번호 하나만 보내고, 여러 번의 조회와 취합은 모두 프록시가 서버에서 대신 처리한 뒤 완성된 목록을 한 번에 돌려줍니다.

이 방식은 농장 단위 조회의 부재를 우회하는 현실적인 방법이지만, 호출 횟수가 늘어난다는 부담이 있습니다. 따라서 다음 세 가지 보완 장치를 함께 두는 것이 중요합니다. 첫째는 캐시로, 한 번 구성한 농가 목록을 일정 시간 저장해 같은 농장에 대한 반복 호출을 줄입니다. 둘째는 호출량 제어로, 한 요청에서 순회하는 범위에 상한을 두어 과도한 호출을 방지합니다. 셋째는 병렬 처리로, 순차로 기다리지 않고 여러 조회를 동시에 보내 응답 시간을 단축합니다.

실제 운영에서는 본인인증을 거친 마이데이터 농가 목록 조회가 가장 정확하므로, 그 연동이 가능해지면 순회 조회 대신 마이데이터 응답을 우선 사용하도록 구성하는 것이 바람직합니다. 이 문서의 순회 방식은 마이데이터 연동 이전, 보유 개체의 일련번호 범위를 알고 있는 경우의 보완책으로 이해하시기 바랍니다.

## 확장 프록시 코드 (Cloudflare Workers)

아래 코드는 기본 가이드의 Workers 프록시에 농장 단위 조회(`svc=farm`) 기능을 더한 것입니다. 캐시와 호출량 제어, 병렬 처리를 모두 반영했습니다.

```javascript
const ANIMAL = "https://apis.data.go.kr/1543061/animalInfoService/animalInfo";

export default {
  async fetch(request, env, ctx) {
    const origin = request.headers.get("Origin") || "*";
    if (request.method === "OPTIONS") {
      return new Response(null, { headers: cors(origin) });
    }

    const url = new URL(request.url);
    const svc = url.searchParams.get("svc");

    // 농장 단위 전체 조회
    if (svc === "farm") {
      const farmNo = (url.searchParams.get("farmNo") || "").replace(/[^0-9]/g, "");
      if (farmNo.length < 7) {
        return json({ error: "농장번호(7자리 이상)를 입력하세요" }, 400, origin);
      }
      const head = farmNo.slice(0, 7);           // 국가3 + 농장4

      // 1) 캐시 확인 (Cloudflare Cache API, 6시간 보관)
      const cacheKey = new Request("https://cache/farm/" + head);
      const cache = caches.default;
      let hit = await cache.match(cacheKey);
      if (hit) return withCors(hit, origin);

      // 2) 일련번호 범위를 순회하며 병렬 조회 (호출량 제어: 한 요청당 최대 200건)
      const MAX = 200;                            // 한 번에 순회할 일련번호 상한
      const BATCH = 20;                           // 동시 호출 수
      const found = [];
      for (let start = 0; start < MAX; start += BATCH) {
        const calls = [];
        for (let i = start; i < start + BATCH && i < MAX; i++) {
          const serial = String(1000 + i).slice(-4);   // 4자리 일련번호
          const trace = head + serial + "0";             // 12자리(체크자리 임시)
          calls.push(lookupOne(trace, env.DATA_GO_KR_KEY));
        }
        const results = await Promise.all(calls);
        for (const r of results) if (r) found.push(r);
        // 연속으로 빈 구간이 길어지면 조기 종료(농가는 일련번호가 연속되는 경향)
        if (results.every((r) => !r) && start > 40) break;
      }

      const body = JSON.stringify({ farmNo: head, count: found.length, items: found });
      const res = new Response(body, {
        headers: { "Content-Type": "application/json", "Cache-Control": "max-age=21600", ...cors(origin) },
      });
      ctx.waitUntil(cache.put(cacheKey, res.clone()));  // 캐시에 저장
      return res;
    }

    // 단건 개체 조회 (기존 기능)
    if (svc === "animal") {
      const traceNo = url.searchParams.get("traceNo");
      const rec = await lookupOne(traceNo, env.DATA_GO_KR_KEY);
      return json(rec || { error: "조회 결과 없음" }, rec ? 200 : 404, origin);
    }

    return json({ error: "알 수 없는 svc" }, 400, origin);
  },
};

// 개체 한 마리 조회 — 존재하지 않으면 null
async function lookupOne(traceNo, key) {
  try {
    const u = ANIMAL + "?serviceKey=" + encodeURIComponent(key)
      + "&traceNo=" + traceNo + "&optionNo=1&_type=json";
    const r = await fetch(u);
    const j = await r.json();
    const it = j?.response?.body?.items?.item;
    const rec = Array.isArray(it) ? it[0] : it;
    if (!rec || !rec.birthYmd) return null;       // 실제 존재하는 개체만
    return {
      cattleNo: traceNo,
      birthYmd: rec.birthYmd,
      sexNm: rec.sexNm,
      lsTypeNm: rec.lsTypeNm,
    };
  } catch (e) {
    return null;
  }
}

function cors(origin) {
  return {
    "Access-Control-Allow-Origin": origin,
    "Access-Control-Allow-Methods": "GET, OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type",
  };
}
function json(obj, status, origin) {
  return new Response(JSON.stringify(obj), {
    status, headers: { "Content-Type": "application/json", ...cors(origin) },
  });
}
function withCors(res, origin) {
  const h = new Headers(res.headers);
  h.set("Access-Control-Allow-Origin", origin);
  return new Response(res.body, { status: res.status, headers: h });
}
```

## 배포와 확인

배포 절차는 기본 가이드와 동일합니다. 코드를 `src/index.js`에 넣고 서비스키를 환경변수로 등록한 뒤 배포합니다.

```bash
wrangler secret put DATA_GO_KR_KEY
wrangler deploy
```

배포가 끝나면 다음과 같이 농장번호로 호출하여 전체 개체가 취합되어 돌아오는지 확인합니다.

```bash
curl "https://minanfarm-proxy.<계정명>.workers.dev/?svc=farm&farmNo=0026027"
```

응답의 `count` 값이 그 농장의 조회된 개체 두수이며, 이 값이 앱에 그대로 반영됩니다. 따라서 실제로 28두가 존재하는 농장이라면 `count`가 28로 돌아오고, 앱의 사육두수도 28두로 표시됩니다.

## 앱과의 연결

minanfarm 앱은 이미 이 확장 프록시와 연동되도록 구성되어 있습니다. 외부 연동 설정에 서비스키와 프록시 주소를 입력하면, 농장번호로 조회할 때 앱이 프록시의 `svc=farm` 경로를 호출하여 실제 농가 개체목록을 받아옵니다. 이때 앱은 추정 데이터를 생성하지 않고 프록시가 돌려준 실제 개체와 두수를 그대로 표시하므로, 추정으로 인한 두수 불일치가 사라집니다. 프록시가 비어 있거나 응답이 없을 때만 기존의 추정 방식으로 자동 전환됩니다.

## 보안 점검

마지막으로 운영 전 다음을 확인하시기 바랍니다. 노출된 서비스키는 개발용이라도 재발급받으시는 것이 안전합니다. 서비스키는 코드가 아닌 서버 환경변수에만 보관하며, 순회 조회의 상한과 캐시 보관 시간을 트래픽 한도에 맞게 조정하시기 바랍니다. 필요하시다면 프록시가 허용하는 출처를 자신의 앱 도메인으로 한정하여 외부의 무단 호출을 차단하실 수 있습니다.
