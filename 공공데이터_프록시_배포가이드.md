# 공공데이터포털 호출 중계 프록시 — 무료 함수형 서버 배포 가이드

이 문서는 minanfarm 앱이 축산물이력제(공공데이터포털) API를 호출할 때 발생하는 두 가지 문제, 즉 브라우저의 교차 출처(CORS) 차단과 서비스키 노출을 동시에 해결하기 위한 서버리스 프록시의 구성 방법을 정리한 것입니다. 프록시는 브라우저 대신 서버에서 공공데이터포털을 호출하여 응답을 앱으로 전달하며, 이 과정에서 서비스키를 서버 측 환경변수에 숨겨 외부에 드러나지 않도록 합니다.

무료로 운영할 수 있는 대표적인 선택지로 Cloudflare Workers와 Vercel Functions 두 가지를 안내합니다. 두 방식 모두 신용카드 없이 무료 등급으로 충분히 운영할 수 있으며, 농장 한 곳의 개체 조회 정도의 트래픽은 무료 한도 안에서 여유롭게 처리됩니다.

---

## 1. 사전 준비

배포에 앞서 다음 세 가지를 준비하시기 바랍니다.

먼저 공공데이터포털에서 발급받은 서비스키를 준비합니다. 앞서 대화에 노출된 키는 보안을 위해 반드시 재발급받으시고, 새로 발급받은 키를 사용하시기 바랍니다. 키는 코드에 직접 적지 않고 서버의 환경변수로만 등록합니다.

다음으로 활용하려는 오퍼레이션의 정확한 엔드포인트를 확인합니다. 개체의 출생일과 품종, 성별은 쇠고기이력정보 서비스에서, 등급과 경락가는 축산물등급판정정보 서비스에서, 사육·이동·도축·유통 통합 이력은 축산물통합이력정보 서비스에서 제공됩니다. 공공데이터포털의 각 서비스 상세 페이지에서 요청 주소와 파라미터명을 확인하여 아래 코드의 해당 부분에 반영하시기 바랍니다.

마지막으로 배포 도구를 준비합니다. Cloudflare 방식은 Node.js와 npm이 설치된 환경이 필요하며, Vercel 방식도 동일합니다.

---

## 2. 방식 A — Cloudflare Workers

Cloudflare Workers는 전 세계에 분산된 서버에서 코드가 실행되어 응답이 빠르고, 하루 10만 건까지 무료로 처리할 수 있어 개인 농가용으로 가장 권장됩니다.

### 2.1 프로젝트 생성

터미널에서 다음을 실행하여 작업 도구를 설치하고 프로젝트를 생성합니다.

```bash
npm install -g wrangler
wrangler login
wrangler init minanfarm-proxy
```

생성된 폴더로 이동한 뒤 `src/index.js` 파일의 내용을 아래 코드로 교체합니다.

### 2.2 프록시 코드 (src/index.js)

```javascript
// 허용할 공공데이터포털 서비스 경로만 화이트리스트로 관리합니다.
const ALLOWED = {
  // 쇠고기이력정보 — 개체 조회(출생일·품종·성별)
  animal: "https://apis.data.go.kr/1543061/animalInfoService/animalInfo",
  // 축산물등급판정정보 — 등급·경락가
  grade:  "https://apis.data.go.kr/1543061/gradeJudgementInfoService/gradeJudgementInfo",
  // 축산물통합이력정보 — 통합 이력
  trace:  "https://apis.data.go.kr/1543061/totalHistoryInfoService/totalHistoryInfo",
};

export default {
  async fetch(request, env) {
    const origin = request.headers.get("Origin") || "*";

    // 브라우저의 사전 요청(preflight) 처리
    if (request.method === "OPTIONS") {
      return new Response(null, { headers: cors(origin) });
    }

    try {
      const url = new URL(request.url);
      const svc = url.searchParams.get("svc");      // animal | grade | trace
      const base = ALLOWED[svc];
      if (!base) {
        return json({ error: "허용되지 않은 서비스입니다" }, 400, origin);
      }

      // 앱에서 전달한 파라미터를 그대로 전달하되, 서비스키는 서버에서 주입
      const params = new URLSearchParams(url.search);
      params.delete("svc");
      params.set("serviceKey", env.DATA_GO_KR_KEY);  // 환경변수에 숨긴 키
      params.set("_type", "json");

      const upstream = base + "?" + params.toString();
      const res = await fetch(upstream, { cf: { cacheTtl: 60 } });
      const body = await res.text();

      return new Response(body, {
        status: res.status,
        headers: { "Content-Type": "application/json", ...cors(origin) },
      });
    } catch (e) {
      return json({ error: "프록시 호출 실패", detail: String(e) }, 502, origin);
    }
  },
};

function cors(origin) {
  return {
    "Access-Control-Allow-Origin": origin,
    "Access-Control-Allow-Methods": "GET, OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type",
  };
}
function json(obj, status, origin) {
  return new Response(JSON.stringify(obj), {
    status,
    headers: { "Content-Type": "application/json", ...cors(origin) },
  });
}
```

### 2.3 서비스키를 환경변수로 등록

키를 코드에 적지 않고 안전하게 보관하기 위해 다음 명령으로 등록합니다. 명령 실행 후 키 입력을 요청하면 재발급받은 새 키를 붙여넣습니다.

```bash
wrangler secret put DATA_GO_KR_KEY
```

### 2.4 배포

```bash
wrangler deploy
```

배포가 완료되면 `https://minanfarm-proxy.<계정명>.workers.dev` 형태의 주소가 출력됩니다. 이 주소가 앱에 입력할 프록시 주소입니다.

### 2.5 동작 확인

브라우저나 터미널에서 다음과 같이 호출하여 정상 응답이 오는지 확인합니다. 실제 개체식별번호를 넣으시면 됩니다.

```bash
curl "https://minanfarm-proxy.<계정명>.workers.dev/?svc=animal&traceNo=002199387420&optionNo=1"
```

---

## 3. 방식 B — Vercel Functions

이미 Vercel을 사용하고 계시거나 GitHub 연동 배포가 익숙하시다면 Vercel Functions도 좋은 선택입니다. 무료 등급에서 월 십만 건 수준의 호출을 처리할 수 있습니다.

### 3.1 프로젝트 구조

빈 폴더를 만들고 그 안에 `api` 폴더를 둔 뒤, 아래 파일을 생성합니다.

```
minanfarm-proxy/
└─ api/
   └─ proxy.js
```

### 3.2 프록시 코드 (api/proxy.js)

```javascript
const ALLOWED = {
  animal: "https://apis.data.go.kr/1543061/animalInfoService/animalInfo",
  grade:  "https://apis.data.go.kr/1543061/gradeJudgementInfoService/gradeJudgementInfo",
  trace:  "https://apis.data.go.kr/1543061/totalHistoryInfoService/totalHistoryInfo",
};

export default async function handler(req, res) {
  res.setHeader("Access-Control-Allow-Origin", "*");
  res.setHeader("Access-Control-Allow-Methods", "GET, OPTIONS");
  res.setHeader("Access-Control-Allow-Headers", "Content-Type");
  if (req.method === "OPTIONS") return res.status(204).end();

  try {
    const { svc, ...rest } = req.query;
    const base = ALLOWED[svc];
    if (!base) return res.status(400).json({ error: "허용되지 않은 서비스입니다" });

    const params = new URLSearchParams(rest);
    params.set("serviceKey", process.env.DATA_GO_KR_KEY);
    params.set("_type", "json");

    const upstream = base + "?" + params.toString();
    const r = await fetch(upstream);
    const body = await r.text();

    res.setHeader("Content-Type", "application/json");
    return res.status(r.status).send(body);
  } catch (e) {
    return res.status(502).json({ error: "프록시 호출 실패", detail: String(e) });
  }
}
```

### 3.3 배포와 환경변수 등록

```bash
npm install -g vercel
vercel            # 최초 배포 — 안내에 따라 프로젝트 생성
vercel env add DATA_GO_KR_KEY   # 키 입력(재발급받은 새 키)
vercel --prod     # 운영 배포
```

배포가 끝나면 `https://minanfarm-proxy.vercel.app` 형태의 주소가 발급되며, 호출 주소는 `https://minanfarm-proxy.vercel.app/api/proxy` 입니다.

---

## 4. 앱과 연결

프록시 주소를 앱의 외부 연동 설정에 입력하면, 앱이 공공데이터포털을 직접 호출하는 대신 이 프록시를 경유하게 됩니다. minanfarm 앱은 서비스키와 함께 프록시 주소를 입력받아 사용하도록 구성할 수 있으며, 이때 호출 형식은 다음과 같습니다.

```
개체 조회:  <프록시주소>/?svc=animal&traceNo=<개체식별번호>&optionNo=1
등급·경락가: <프록시주소>/?svc=grade&traceNo=<개체식별번호>
통합 이력:   <프록시주소>/?svc=trace&traceNo=<이력번호>
```

프록시를 경유하면 브라우저의 교차 출처 제약이 해소되고, 서비스키는 앱이나 브라우저에 노출되지 않은 채 서버에서만 사용됩니다. 따라서 미리보기나 웹 브라우저에서도 실제 개체 정보가 정상적으로 조회됩니다.

---

## 5. 농가 전체 개체 목록과 본인인증

한 가지 유의하실 점은, 위 프록시로 조회하는 쇠고기이력정보는 개체식별번호로 한 마리씩 조회하는 공개 서비스라는 것입니다. 한 농장이 보유한 개체 전체 목록을 농장번호만으로 한꺼번에 받는 것은 개인정보에 해당하므로, 축산물이력제 마이데이터에서 농장경영주 본인인증을 거쳐야 합니다.

따라서 실제 운영에서는 두 경로를 함께 사용하시는 것이 적절합니다. 보유 개체의 식별번호를 이미 알고 계신 경우에는 위 프록시로 각 개체의 정확한 정보를 조회하여 채우고, 농가 전체 목록을 자동으로 받아오려면 마이데이터 본인인증 연동을 추가로 구성하시면 됩니다. 마이데이터 연동 역시 동일한 프록시 구조 위에서 인증 토큰을 전달하는 방식으로 확장할 수 있습니다.

---

## 6. 보안 점검 항목

마지막으로 운영 전 다음을 확인하시기 바랍니다. 노출되었던 서비스키는 반드시 폐기하고 재발급받으셔야 합니다. 서비스키는 코드나 앱이 아닌 서버 환경변수에만 보관하며, 프록시가 허용하는 서비스 경로를 화이트리스트로 제한하여 의도하지 않은 외부 호출에 악용되지 않도록 합니다. 필요하시다면 프록시에 허용 출처를 자신의 앱 도메인으로 한정하는 설정을 추가하여 보안을 한층 강화하실 수 있습니다.
